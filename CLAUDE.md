# CLAUDE.md

## このプロジェクト

`SPEC.md` に書かれた給料メーターの実装。フレームワーク・ビルド・依存パッケージなし。
`index.html` を `file://` で直接開いても動くことが要件なので、ES モジュールは使わず
`<script>` 分割 + `WT` 名前空間で構成している。この前提は崩さないこと。

公開先: https://katsumi9152.github.io/wage-ticker/ (GitHub Pages / `main` ブランチの root)

## 変更したら必ずテストを実行する

コードを直したら、報告する前・push する前に必ず実行する。ユーザーはテストの実行と
確認を Claude に任せている。結果(件数と、落ちたテスト名)は毎回そのまま伝えること。

```powershell
pwsh -File test\run.ps1
```

- 全部通れば終了コード 0、1件でも落ちれば 1
- 落ちたまま push しない。直すか、直せない理由を先に伝える
- ロジックを変えたら、対応するテストを `test/tests.js` に足す
- 画面の挙動(ボタン・表示・保存)を変えたら `test/smoke.js` に足す
- この環境には Node も Python も無い。テストは Windows 同梱の cscript で動かしている

## 構造の約束

- `src/app.js` だけが DOM に触れる。それ以外(`wage.js` / `aggregate.js` / `period.js` /
  `time.js` / `holidays.js` / `storage.js`)は DOM に依存しない純粋関数に保つ
- 割増率・しきい値・デフォルト設定は `src/constants.js` に集約する。他の場所に数値を直書きしない
- 重い区分計算は 1 秒ごと、金額の描画は `requestAnimationFrame`。この分離を壊さない
- 色は `styles.css` の `:root` トークン経由でのみ使う。コンポーネントに生の色を書かない。
  テーマは3状態(端末追従 / light / dark)で、既定は `data-theme` を付けない
- 1日の所定労働時間は入力項目ではなく、勤務時間 − 休憩時間の自動算出
  (`wage.deriveDailyScheduledHours`)。入力欄を復活させない
- 金額はぼかさない(覗き見防止は廃止済み。SPEC 11 を復活させない)

## キャッシュ対策とバージョン表示

`index.html` の `<script>` / `<link>` はすべて `?v=YYYYMMDDx` を付けている
(GitHub Pages のキャッシュを回避するため)。`styles.css` や `src/*.js` を
1文字でも変えたら、このバージョン文字列を必ず全箇所まとめて上げること
(sed で一括置換でよい)。上げ忘れると「直したのに反映されない」に見える。

画面下の `.app-version`(`index.html` の `<p class="app-version">`)にも同じ
バージョン文字列を表示している。ユーザーが実機で「最新が反映されているか」を
自分で確認できるようにするためのもの。`?v=` を上げたら、この表示文字列も
必ず同じ値に揃えること。

## SPEC から意図的にずらした点

README の「SPEC からの判断・補足」にまとめてある(固定残業代・祝日など、SPEC に無い
追加項目も含む)。仕様と実装が食い違って見えても、まずそこを確認すること。勝手に
「仕様どおり」に戻さない。

## ユーザーとのやりとり

- 説明は日本語で、専門用語にはそのつど短い補足をつける
- git の push は毎回ひとこと確認してから行う(コミットまでは進めてよい)
