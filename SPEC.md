# SPEC.md — 今いくら?(リアルタイム給料メーター)

## 1. 概要・目的

月給制・フレックスタイム制(コアタイムなし)の従業員が、出勤するだけで金額と労働時間がリアルタイム(秒単位)で積み上がっていくWebアプリ。Android / iPhone を問わずブラウザだけで動作する。

### 1.1 目的(最優先事項)

**このアプリの目的は、秒単位で加算される金額を見て、働くモチベーションを高めること。**

**設計三原則: 計算は極力正確に、操作は単純に、表示はシンプルに。**
このアプリは本業(仕事そのもの)の"わき役"であり、主役ではない。複雑さはすべて裏側(計算ロジック)に閉じ込め、表側(操作・表示)には一切出さない。

- **計算は極力正確に**(裏方): 給与計算ロジック(所定内/法定内残業/法定時間外/深夜/休日/前月比較/36協定など、3〜9章)は、就業規則・労基法に照らして精度を追求してよい。ここで手を抜くと「見ていて信頼できない数字」になり、モチベーションどころか不信感につながる
- **操作は単純に**(最小限のボタン): 基本は「出勤/退勤」の2ボタンのみ(休憩は打刻せず自動控除)。さらに徹底したい人向けに、ボタン操作すら不要な自動モードも用意する(5章)。設定・カレンダー指定・内訳確認は、普段は隠れていて必要なときだけ開ける場所に置く(10章)
- **表示はシンプルに**(主役は数字そのもの): 画面に常時出す数字は「秒単位の増加」「今日いくら」「今月いくら(+前月比)」の3つに絞る。裏側の計算がどれだけ複雑になっても、ユーザーの目に触れる情報量は増やさない

### 1.2 想定利用シーン

以下の2つの「ちょっと見る」瞬間のために作る。それ以外の機能は、この2つの体験を邪魔しない限りにおいて存在してよい。

- **昼休みにふと開く**: 「あっ、おれ稼いでるな」という静かな満足感。派手な演出は不要
- **残業中に開く**: 「よし、頑張ろう」という前向きな気持ち。割増時間帯は、通常より単価が上がっている=より稼げているタイミングとして**警告ではなく前向きな合図**で見せる(色味の変化程度に留め、警告色・通知音は使わない)

いずれの瞬間も**「開く→数字を見る→閉じる」までが数秒で完結する**ことを前提に設計する。ページの読み込み速度、ぼかし解除(11章)からメイン数値が読み取れるまでの体感速度は、この短い接触時間に耐えられるよう軽量に保つこと。

### 1.3 スコープ外・免責

- **非対象**: 社会保険料・所得税・住民税などの控除計算、年末調整、賞与計算。本アプリは「額面(グロス)の労働対価」のみを可視化する
- **免責**: 本仕様は一般的に用いられる計算方法の実装であり、法律相談・労務相談の代替ではない。実際の運用では会社の就業規則・36協定の内容を優先すること
- **実際の給与との差異について**: 本アプリの出退勤時刻は会社の勤怠システムとは別に、このアプリ上で個人が打刻するものであり、実際の会社の記録とは一致しない。表示される金額は「体感用の目安」であり、給与明細の金額と一致しないことは仕様として許容する。給与計算の正式な代替物ではないことをUI上(設定画面や初回起動時の一言)でも示すこと

---

## 2. 用語定義

| 用語 | 意味 |
|---|---|
| 基本給 | 月額で支給される固定給(円/月) |
| 所定労働時間 | 会社が定める1日の労働時間(休憩除く) |
| 月平均所定労働時間 | 1ヶ月あたりの平均所定労働時間。割増賃金の基礎単価を出すために使う法定の考え方 |
| 基礎時給単価 | 割増賃金の計算のベースとなる1時間あたりの単価 |
| 所定労働時間の総枠 | 「1日の所定労働時間 × 清算期間内の所定労働日数」。会社によっては法定(週40h)より短い場合がある(例: 1日7時間・週35時間) |
| 法定労働時間の総枠 | 法律上働ける時間の上限(週40時間 × 清算期間の暦日数 ÷ 7)。所定労働時間の総枠と一致するとは限らない |
| 所定内労働 | 所定労働時間の総枠におさまる労働(基本給で既にカバーされている時間) |
| 法定内残業(所定超・法定内) | 所定労働時間の総枠は超えたが、法定労働時間の総枠にはまだ収まっている労働。法律上の割増義務はなく、基礎時給単価と同額(1.00倍)で追加支払いするのが一般的 |
| 法定時間外労働(法定超残業) | 法定労働時間の総枠を超えた労働。ここから25%以上の割増が法律上必須 |
| 深夜労働 | 22:00〜翌5:00の労働 |
| 所定休日 | 会社カレンダー上の休みの日全体(週休二日制など)。この中に「法定休日」と「所定休日(法定超)」の2種類が混在する |
| 法定休日 | 所定休日のうち、労基法で最低週1日(または4週4日)与える義務がある休日。多くの会社は曜日で固定(例: 日曜日) |
| 所定休日(法定超) | 所定休日のうち法定休日でない方(例: 完全週休二日制の土曜日)。ここでの労働は35%増の対象ではなく、通常の所定内/法定内残業/法定時間外の枠組みで計算する |
| 法定休日労働 | 法定休日として指定した日に行う労働。35%以上の割増の対象 |
| 時間外労働の原則上限(36協定) | 月45時間・年360時間が原則の上限(特別条項がある場合は年6回まで月45時間を超えられ、年720時間等の上限がある)。60h超で割増率が変わるラインとは別の、労務管理上の目安ライン |
| フレックスタイム制(コアタイムなし) | 1日の中で必ず勤務すべき時間帯(コアタイム)を設けず、清算期間内の総労働時間で管理する制度。1日8時間超という日次の残業判定は使わない |
| 清算期間 | フレックスタイム制で労働時間を清算する期間。本アプリでは賃金締め日を基準とした約1ヶ月とする |
| 賃金締め日 | 給与計算の対象期間を区切る日(例: 毎月20日締め)。締め日の翌日から翌月の締め日までが1つの集計期間になる |
| 有給休暇 | 取得した日は出勤しなくても所定労働時間分を働いたものとして賃金が支払われる休暇 |
| 会社休日 | カレンダー上「出勤の対象外」として指定した日。指定時に「法定休日」か「所定休日(法定超)」かを選ぶ(デフォルトは設定した法定休日の曜日から自動判定) |
| 未入力日 | 清算期間内の所定労働日のうち、出退勤の打刻も有給・会社休日の指定もされていない日 |

---

## 3. 給与計算ロジック(標準式)

### 3.1 月平均所定労働時間・基礎時給単価

```
年間所定労働日数 = 365 - 年間所定休日数
月平均所定労働時間 = 年間所定労働日数 × 1日の所定労働時間 ÷ 12
基礎時給単価 = 基本給 ÷ 月平均所定労働時間
```

※ 単純な「基本給 ÷ 稼働日数 ÷ 勤務時間」と違い、月平均所定労働時間ベースなら年間を通じて単価が一定になる(多くの企業の給与規程・残業代計算がこの方式)。うるう年は365固定でよい。

### 3.2 所定/法定の総枠(清算期間ごと)

フレックスタイム制(コアタイムなし)のため、**1日8時間超・週40時間超という日次/週次の残業判定は行わない**。かわりに清算期間単位で、実働時間を2つの枠と比較する。

```
清算期間の暦日数 = 締め日を基準とした当該期間の日数(例: 8/21〜9/20なら31日)

所定労働時間の総枠(時間) = 1日の所定労働時間 × 清算期間内の所定労働日数
法定労働時間の総枠(時間) = 40 × 清算期間の暦日数 ÷ 7
```

会社の所定労働時間が法定と同じ、または法定より長く設定されることは通常ない(違法)。そのため通常は **所定労働時間の総枠 ≦ 法定労働時間の総枠**。実働合計が所定労働時間の総枠を超えた部分から「法定内残業」、さらに法定労働時間の総枠を超えた部分から「法定時間外労働(割増必須)」として扱う。

### 3.3 割増率(法定の下限。就業規則でこれ以上に設定されている場合はそちらを優先)

会社の所定労働時間が法定(週40h)より短い場合、**所定労働時間の総枠を超えても、まだ法定労働時間の総枠内であれば、割増賃金の法的な支払い義務はない**(=法定内残業)。ただしこの時間も基本給の対象外なので、多くの企業では基礎時給単価と同額(1.00倍)で別途支払う。

| 種別 | 割増率 | 適用条件 |
|---|---|---|
| 所定内労働 | 0(追加支払いなし) | 所定労働時間の総枠内。基本給で既にカバー済み |
| 法定内残業(所定超・法定内) | 1.00 | 所定労働時間の総枠は超えたが、法定労働時間の総枠にはまだ収まる部分。会社の所定労働時間が法定と同じ会社では発生しない |
| 法定時間外労働(法定超・月60hまで) | 1.25 | 法定労働時間の総枠を超えた労働のうち、その月の残業累計が60h以下の部分 |
| 法定時間外労働(法定超・月60h超) | 1.50 | 同一月の残業累計が60hを超えた部分 |
| 深夜労働 | +0.25(他の割増と加算) | 22:00〜翌5:00 |
| 法定休日労働 | 1.35 | **法定休日**として登録した日に行った労働のみ。**所定休日(法定超)の出勤はここに含めず、通常の所定内/法定内残業/法定時間外の枠組みで計算する** |
| 法定休日 + 深夜 | 1.60 | 上記の組み合わせ |
| 時間外 + 深夜 | 1.50 | 法定超の時間外かつ22-5時 |

計算式: `その分の金額 = 基礎時給単価 × 割増率 × 該当時間(時間単位)`

※ 所定内労働は「基本給に含まれるため追加計算しない」という意味。リアルタイム表示上は基礎時給単価分をそのまま積み上げてよい(基本給の日割り取り崩しとして見せる)が、月次の「追加で発生した残業代」の集計とは区別すること。

※ **36協定との関係**: 月45時間・年360時間という原則上限、単月100時間未満・複数月(2〜6ヶ月)平均80時間以内・月45時間超は年6回まで、という複数月にまたがるチェックも本来必要になる。8章で詳述する。

---

## 4. 初期設定項目(セットアップ画面)

| 項目 | 型 | 説明 | デフォルト値(上場企業に多いパターン) |
|---|---|---|---|
| 基本給(月額) | 数値(円・マスク入力) | 必須。パスワード欄と同様に見えないよう表示(11章)。表示/非表示切り替えの目アイコンを添える | 空欄(個人差が大きいためデフォルトなし。プレースホルダーとして300,000程度を例示) |
| 1日の所定労働時間 | 数値(時間) | 必須。基礎時給単価と有給1日分の換算に使用 | 7.5時間(7時間30分) |
| 年間所定休日数 | 数値(日) | 必須 | 120日(完全週休二日制+祝日+夏季+年末年始) |
| 賃金締め日 | 数値(1〜31、または「末日」) | 必須。月次集計の期間の区切りに使用 | 末日(月末締め) |
| 法定休日の曜日 | 曜日選択 | 必須。会社休日をカレンダー指定する際、この曜日はデフォルトで「法定休日」、それ以外はデフォルトで「所定休日(法定超)」として扱う(個別に変更可) | 日曜日 |
| 休憩時間帯(任意) | 時刻の範囲、または未設定 | 登録すれば、出退勤の打刻区間がこの時間帯と重なったときだけ自動控除する(5.2)。**未設定の場合は、実働時間から一律60分を控除する**。打刻(ボタン)による休憩の計測は行わない | 12:00〜13:00(登録する場合の一般的な例) |
| 勤務間インターバルの目安時間 | 数値(時間) | 任意。8.2の気づき表示のしきい値 | 11時間 |
| 自動モード | オン/オフ | オンにすると出勤/退勤ボタンが不要になり、標準勤務スケジュールに沿って自動的にカウンターが動く(5.3) | オフ(まずは手動の2ボタンで慣れてもらい、任意でオンにする) |
| 標準勤務スケジュール(自動モード時のみ) | 出勤予定時刻・退勤予定時刻・勤務予定の曜日 | 自動モードの基準となるスケジュール | 1日の所定労働時間・法定休日の曜日設定から仮の値を自動算出(例: 9:00〜17:30、月〜金) |

### 4.1 デフォルト値の根拠

厚生労働省「就労条件総合調査」(企業規模1,000人以上、上場企業の多くが含まれる層)によると、1日の所定労働時間の実態値は概ね7時間43分〜7時間47分、週所定労働時間は約38時間53分〜39時間で推移している。本アプリでは覚えやすいきりの良い値として**7.5時間(週37.5時間)**をデフォルトに採用する。

年間休日総数は同調査(企業規模1,000人以上)で116.6日という実態値があるが、一般に「大手企業の目安」として案内される日数(完全週休二日制+祝日+夏季休暇+年末年始で概ね120〜125日)の方が馴染みがあるため、中間的な**120日**をデフォルトとする。

いずれも初期設定画面でその場で自由に変更できる値であり、あくまで「よくあるパターン」の初期値に過ぎない。

---

## 5. 出退勤・休憩・自動モードのロジック

### 5.1 状態管理(基本形)

```
State = {
  status: "off" | "working",
  clockInAt: timestamp | null,       // 出勤を押した時刻
  isLegalHoliday: boolean,           // 出勤時点でこの日が「法定休日」に該当するか(4章の設定から自動判定、上書き可)
  lastClockOutAt: timestamp | null,  // 前回「退勤」を押した時刻(勤務間インターバルの算出用、8.2)
  overtimeFlag: boolean              // 自動モードで「残業開始」が押されたか(5.3)
}
```

- ボタンは常に「出勤」「退勤」の2つだけ(自動モード時は5.3参照)
- `clockInAt` はブラウザのローカルストレージに保存し、ブラウザを閉じて再度開いた際もそこから経過時間を再計算する
- リアルタイム表示は `requestAnimationFrame` 等で高頻度に再計算する

### 5.2 休憩時間の自動控除(事前登録 or 一律1時間)

**休憩は打刻せず、あらかじめ登録した時間帯を自動的に差し引く。1本のシンプルな仕組みに統一する。**

```
if 休憩時間帯が登録されている:
    その日の実働時間 = (退勤時刻 − 出勤時刻) − (登録した時間帯と重なっていればその時間帯の長さ、重ならなければ0)
else:
    その日の実働時間 = (退勤時刻 − 出勤時刻) − 60分
```

休憩時間帯を登録していても、打刻区間がその時間帯と重ならない日(例: 午前は半休で13:00から出勤するケース)は、控除が発生せず実働時間がそのまま計上される。これが実態(その日は休憩を取らなかった)と一致していれば正しい挙動であり、8.2の「休憩未取得の目安」表示はこのケースで意味を持つ。

### 5.3 自動モード(スケジュール登録による全自動化)

**「出勤/退勤ボタンすら押さず、アプリを開くだけでよい」という究極の単純化を、任意のモードとして用意する。1.1の「操作は単純に」の最終形。**

- 4章で「標準勤務スケジュール」を登録し、「自動モード」をONにすると、出勤/退勤ボタンは画面から消える
- アプリを開くと、現在時刻と登録スケジュールを比較し、勤務予定時間内であれば自動的にカウンターが動いている状態になる。休憩時間帯(5.2)はカウンターが止まる。ボタン操作は一切不要
- 退勤予定時刻になると、カウンターは自動的に停止する。これが「毎日ほぼ同じ時間だけ働く人」にとってのデフォルトの挙動。**この自動モードは、基本的には残業をしない/あまりしない人向け**の割り切った機能である
- **残業する日への対応**: 勤務時間中ずっと「残業開始」ボタンを小さく表示する。**退勤予定時刻を迎える前に**このボタンを押しておけば自動確定せず、カウンターが動き続ける(=時間外労働を継続計測)。実際に終えたタイミングで(このときだけ現れる)「残業終了」ボタンを押して確定する
- 「残業開始」を押さないまま退勤予定時刻を迎えた場合は、そのまま自動的に確定する。後から気づいても自動モードの範囲では取り消せない(5.9の打刻忘れ編集で手動修正は可能)
- **予定と違う日**(遅刻・早退・半休・出張など): 自動モードのままだと実態とズレるので、その日だけ手動で実際の時刻に修正できるようにする(5.9と同じ編集UIを流用)
- 有給休暇・会社休日として指定した日(6章)は、自動モードでもカウンターは動かない
- 自動モードは4章でいつでもON/OFF切り替え可能。OFFにすれば5.1・5.2の手動(出勤/退勤ボタン)方式に戻る

### 5.4 リアルタイム金額の算出(擬似コード)

```
function calcEarnings(state, now, settings, periodSoFarMinutes, periodOvertimeSoFarMinutes):
    // その日の実働時間(リアルタイム表示中は「まだ控除前」の経過時間をそのまま見せてよい。
    // 休憩時間帯に入った瞬間、控除予定分をなめらかに差し引くような見せ方も可、10章)
    rawMinutes = (now - state.clockInAt)
    if 休憩時間帯が登録されている:
        deductedMinutes = 登録した時間帯と[state.clockInAt, now]が重なっていればその時間帯の長さ、重ならなければ0
    else:
        deductedMinutes = 60
    workedMinutesThisSession = rawMinutes - deductedMinutes

    // 所定/法定の2つの枠との比較で区分を判定(1日8h超という日次判定はしない)
    scheduledFrameMinutes = 所定労働時間の総枠(分)
    legalFrameMinutes = 法定労働時間の総枠(分)

    所定内区分の分数 = min(workedMinutesThisSession, max(0, scheduledFrameMinutes - periodSoFarMinutes))
    残り = workedMinutesThisSession - 所定内区分の分数
    法定内残業区分の分数 = min(残り, max(0, legalFrameMinutes - max(periodSoFarMinutes, scheduledFrameMinutes)))
    法定時間外区分の分数 = 残り - 法定内残業区分の分数
    法定時間外区分の分数を、periodOvertimeSoFarMinutesを踏まえて「60h未満/60h超」にさらに分解する
    深夜と重なる分数(上記各区分との重複を加算換算、5.5)
    state.isLegalHolidayがtrueの場合のみ、深夜以外を1.35倍・深夜重複を1.60倍に置き換える(所定休日(法定超)の場合はこの置き換えを行わず、通常の所定内/法定内残業/法定時間外のまま計算する)

    金額 = Σ (各区分の時間(h) × 基礎時給単価 × 該当割増率)
    return { workedMinutesThisSession, 金額 }
```

### 5.5 深夜時間帯の重なり判定

簡易版で実装済みの「22:00〜翌5:00とのオーバーラップ計算」ロジックをそのまま流用可能。これに「所定内か法定内残業か法定時間外か」「休日か平日か」の軸を掛け合わせて割増率を決定する。

---

## 6. 有給休暇・会社休日・未入力日

### 6.1 有給休暇・会社休日の指定

- カレンダー(または日付選択)から、任意の日を「有給休暇」または「会社休日」として指定できる
- **有給休暇**の日: 出退勤の打刻なしで、1日の所定労働時間分をまるごと「所定内労働」として計上する(金額 = 基礎時給単価 × 1日の所定労働時間)。時間外・深夜・休日の割増対象にはしない。※有給休暇の賃金は「通常の賃金を支払う方式」を前提とする(月給者において最も一般的。平均賃金方式・標準報酬月額方式は対象外)
- **会社休日**の日: その日は「所定労働日」から除外する。打刻がなくても未入力日としては扱わない。指定時に「法定休日」か「所定休日(法定超)」かを選ぶ(4章の設定からデフォルト判定、個別上書き可)
- 会社休日に出勤した場合(打刻あり)は、法定休日か所定休日(法定超)かによって計算方法が変わる(3.3参照): 法定休日なら35%増、所定休日(法定超)なら通常の所定内/法定内残業/法定時間外の枠組み

### 6.2 未入力日の扱い

清算期間の途中で集計を表示する際、まだ日付が来ていない未来の日は集計に含めない。一方、**すでに過ぎた所定労働日のうち、出退勤の打刻も有給・会社休日の指定もされていない日(未入力日)** は、次のルールで自動的に加算する。

- 未入力日は「1日の所定労働時間 × 所定内(割増なし・残業なし)」として加算する
- これは「打刻を忘れただけかもしれないし、出張などでそもそも打刻するつもりがなかったのかもしれない」という保守的な前提に基づく仮の値。**理由を問わず、未入力の日は一律で「所定内・残業なし」として扱う**(多くの会社が出張時などに実際そう運用している)
- 月次サマリーの表示では、未入力日から自動加算された時間・金額であることが分かるよう区別して表示する(例: 「うち未入力自動加算分 ◯時間」)

### 6.3 打刻忘れの追記(任意・強制しない)

- 未入力日はいつでも後から実際の出退勤時刻を追記でき、追記するとその日は通常の勤務日と同様に3〜5章のロジックで再計算される
- **ただし追記は必須ではない。** 出張などで最初から打刻するつもりがない日について、アプリ側からの入力催促・通知は行わない(未入力=出張等の意思表示として扱ってよい)
- 未入力日は6.2の通り自動的に「所定内・残業なし」で確定するため、追記しなければそのまま金額が確定するだけで、追加のアクションを要求しない
- UI上「未入力日が◯日あります」といった一覧は補助情報(10章の折りたたみ)として見られるが、催促や警告の体裁にはしない

---

## 7. 月次サマリー

### 7.1 清算期間サマリー(「今月いくら」)

「今月いくら」はメイン画面に常時表示する3指標の1つ(10章)。常に「現時点まで」の値として以下を計算し表示する。

```
清算期間の実働合計時間 = Σ(打刻から確定した実働時間) + Σ(有給の日の所定労働時間) + Σ(未入力日の所定労働時間)
清算期間の合計金額 = Σ(各区分の金額)
```

締め日を過ぎていない限り、この値は「月途中の速報値」として扱う。UI上は「◯月◯日締め・あと◯日」といった形で、集計がまだ途中であることが分かるようにする。

### 7.2 前月比較(先月に比べてどれだけ働いているか)

**「先月に比べてどのくらい働いているか」は、金額の絶対値と並ぶ重要な指標。** 単純に「今月の合計」と「先月の合計(確定値)」を比べても、月の途中では先月の方が必ず多く見えてしまい意味がないため、**同じ経過日数時点同士**で比較する。

```
今月の経過日数 = 今日 − 清算期間の開始日 + 1
比較対象 = 先月の清算期間のうち、開始日から同じ日数分だけ経過した時点までの実績(時間・金額)
今月との差 = 今月ここまでの実績 − 比較対象(先月の同日数時点の実績)
```

- 清算期間が締まった直後(月初)は先月の「同日数時点」データが1日分などごく少量になるが、それでよい(同条件同士の比較を優先)
- 表示は「先月の同時点より+○○円 / +○時間」のような差分を基本とし、+ならポジティブなトーン、−でも責めるようなトーンにはしない
- 月が完全に終わった後は、先月の確定合計と今月の確定合計をそのまま比較してよい(按分不要)

### 7.3 データ保持方針(詳細ログは直近2期間、残業時間の要約は12ヶ月)

**個人データはブラウザ内にしか無い(11章)ため、際限なく貯め続けるとストレージを圧迫する。日別の詳細ログは「今月(進行中)」と「先月(確定済み)」の2期間分だけを保持する。**

- 締め日を迎えたら: 「先月」の詳細ログは削除し、直前まで「今月」だったログを新しく「先月」として保持し直す。新しい清算期間の「今月」ログはゼロから開始する
- 削除は自動的に行い、確認や警告は出さない
- 日別の内訳は先月分まで保持すればよく、前々月以前の日別内訳は不要

**ただし例外として、残業時間サマリー(7.4)だけは12ヶ月分を軽量に保持する。** 36協定の複数月・年間チェックには「その月の残業時間の合計」という1つの数字だけあれば足りるため、ストレージへの影響はごくわずか。

### 7.4 残業時間サマリー履歴(36協定の複数月・年間チェック用)

前々月以前の詳細ログを消しても、36協定の遵守状況(単月100時間未満・複数月平均80時間以内・月45時間超は年6回まで・年360〜720時間)を確認できるよう、**清算期間ごとの「残業時間の合計」だけを軽量な配列として直近12ヶ月分保持する**。

```
残業時間サマリー履歴 = [
  { 清算期間ラベル: "2026-07", 法定内残業時間: n, 法定時間外時間: n, 実働合計時間: n },
  { 清算期間ラベル: "2026-06", ... },
  ... (最大12件、古いものから自動で削除)
]
```

締め日を迎えるたびに、確定した清算期間の残業時間だけをこの履歴に追記し(日別内訳は含めない)、13ヶ月目に入る古いレコードを削除する。この履歴を使って、折りたたみの補助情報(10章)に以下を表示できる:

- 直近12ヶ月の年間残業時間累計(年360〜720時間の目安との対比)
- 月45時間超えが年に何回あったか(年6回までという原則の目安)
- 直近2〜6ヶ月の平均残業時間(複数月平均80時間以内の目安)

これらはあくまで**参考情報**であり、正式な36協定の管理・届出は会社の労務担当・勤怠システムが行うべきものである(1.3の免責を参照)。

---

## 8. 労務コンプライアンスの「気づき」表示

8.1(36協定)と8.2(休憩・インターバル)は同じ考え方で、**気づける程度の軽い表示**として追加する。強制も催促もせず、折りたたみの補助情報として、確認したい人が見られる場所に置く(1.2のトーンを踏襲: 警告色・通知は使わない)。

### 8.1 36協定の目安表示

- 月60h到達(割増率変更、3.3)に加え、月45h到達(36協定の原則上限の目安)も分けて表示する
- 7.4の履歴を使い、直近12ヶ月の残業時間累計・月45時間超えの年間回数・直近2〜6ヶ月の平均残業時間も表示する
- 金額計算そのものには影響しない参考情報

### 8.2 休憩未取得・勤務間インターバルの目安

**① 休憩未取得の目安**
- 労基法上、実働6時間超で45分以上・8時間超で60分以上の休憩を労働時間の途中に与える義務がある。よくあるのは「午前は半休、午後だけ6時間以上働いたのに休憩を挟んでいない」というケース
- その日の実働時間が6時間・8時間の節目を超えた時点で、休憩の自動控除(5.2)が発生していなければ、「休憩未取得」の目安として折りたたみ内に表示する(例: 「本日は休憩の控除なしで6時間を超えています」)
- あくまで表示のみで、ポップアップや音での通知はしない

**② 勤務間インターバルの目安**
- 「勤務間インターバル」= 前回の退勤時刻から、次回の出勤時刻までの間隔。労働時間等設定改善法上は努力義務(法的強制力のある一律の時間数はない)だが、一般に11時間程度が目安として案内される
- 前回の退勤時刻から次の出勤時刻までの間隔を計算し、設定した目安時間(4章、デフォルト11時間)を下回っていれば、折りたたみ内に「前回の退勤から◯時間◯分」のように表示する
- こちらも催促・警告はせず、記録として見られるだけにとどめる

---

## 9. 一般的な就業規則・労基法との整合性チェック(精査結果まとめ)

**見直して仕様に反映した点**
- 「法定休日」と「所定休日(法定超)」を区別し、35%増の対象を法定休日出勤のみに限定した(3.3, 6.1)
- 有給休暇の賃金算定は「通常の賃金を支払う方式」を前提とすることを明記した(6.1)
- 36協定の原則上限(月45時間・年360時間、複数月平均80時間、単月100時間未満)を、単月だけでなく直近12ヶ月の履歴でも表示できるようにした(7.4, 8.1)
- 休憩の法定付与ライン(実働6時間超45分・8時間超60分)と、勤務間インターバルの目安(努力義務、目安11時間)を、警告ではなく「気づき」レベルの表示として追加した(8.2)

**簡略化のまま残した点(将来拡張候補)**
- **時間外労働の端数処理**: 通達上、1ヶ月の時間外・休日・深夜労働の合計時間に1時間未満の端数がある場合、30分未満切り捨て・30分以上切り上げが認められている(日々の切り捨ては不可)。本アプリは秒単位のリアルタイム表示が目的(1.1)であり、正式な給与計算の代替ではない(1.3参照)ため、この端数処理ルールは適用せず、そのまま実数値を表示・積算する
- **法定休日の振替・代休**: 振替休日や代休の扱いは対象外。単純に「その日が法定休日かどうか」だけで判定する

---

## 10. UI要件

### 10.1 画面の優先順位(1.1参照)

**常に見えているべき数字は3つだけ: 「秒単位でいくら稼いだか」「今日いくらか」「今月いくらか」。この3つが最重要指標であり、他の要素はすべてこの3つを支える補助情報。**

**加えて、「今月」の数字には常に「先月の同時点との差」を小さく添える**(7.2)。

1. **最優先**: 秒単位でなめらかに増え続ける金額表示。更新頻度・アニメーションの滑らかさを最も重視する
2. **同列で常時表示**: 「今日の金額」「今月の金額 + 先月同時点との差分バッジ」。折りたたまず常に見えている状態にする
3. **次点**: 出勤/退勤ボタンなど、その場で必要な操作
4. **補助情報(デフォルト折りたたみ)**: 所定内/法定内残業/深夜割増などの内訳、締め日までの残り日数、36協定の目安表示(8.1)、休憩未取得・勤務間インターバルの目安(8.2)、未入力日の一覧、先月の確定合計そのもの
5. **設定操作(普段は隠す)**: 初期設定値の変更、有給休暇・会社休日のカレンダー指定

「今日」「今月」以外の期間区分(週次・年次など)や、内訳の詳細は積極的に増やさない。補助情報や設定は「見たい人が開けばよい」もの。

### 10.2 個別要件

- 簡易版のデザイン(LCD/計器盤モチーフ、金額のリアルタイム表示)を踏襲
- ボタンは常に「出勤 / 退勤」の2つだけ(5.2)。自動モード(5.3)がONの場合はこの2ボタンも消え、勤務時間中は「残業開始」ボタンだけを小さく置き、押すと「残業終了」ボタンに切り替わる
- メイン画面は3つの数字を軸に構成する: **①秒単位で増え続ける金額(最大・最目立つ)、②今日の金額、③今月の金額**。②③は折りたたまず常時表示するが、①より小さい扱いにする
- ③今月の金額のすぐ横に、先月の同時点との差分を小さく添える(7.2)。プラス/マイナスで色を変える程度に留め、警告的な見た目にはしない
- 実働時間(今日/今月)は金額に添える補助数値として表示してよいが、あくまで金額が主役
- 出勤時ではなく、カレンダー画面から任意の日を「有給休暇」「会社休日」として指定できるUIを用意する(設定操作の一部)
- 内訳表示(任意で開閉、デフォルト非表示): 所定内○○円 / 法定内残業○○円 / 法定時間外割増○○円 / 深夜割増○○円 / うち未入力自動加算分○○時間
- 設定画面に4章の各項目を配置する
- 折りたたみ内に、8.1(36協定)・8.2(休憩未取得・勤務間インターバル)の気づき表示を置く。いずれも警告色・通知は使わない
- 締め日までの残り日数を表示し、集計が「速報値(月途中)」か「確定値(締め後)」かが分かるようにする(折りたたみ内)
- 会社休日をカレンダーで指定する際、「法定休日」か「所定休日(法定超)」かを選べるようにする

---

## 11. 覗き見防止(プライバシー)モード

**このアプリは電車内・オフィス・休憩室など、他人の視界に入りやすい場所で開かれる前提で設計する。基本給や積み上がった金額は、隣の人にちらっと見られても分からないようにする。**

- **金額表示(①秒単位/②今日/③今月)はアプリを開くたびデフォルトで「ぼかし表示」から始まる。** CSSのぼかし(blur)、または「¥•••,•••」のようなマスク文字にする
- ぼかした状態でも、**数字が増えていく「動き」自体は見えるようにする**(ぼけた光の粒がじわっと大きくなる、脈動するなど)。金額は読み取れないが「今稼いでいる感覚」は残し、1.1の目的を損なわない
- 金額のすぐそばに目のアイコンを置き、**タップしている間だけ**、または**1回タップで数秒間だけ**表示する(離す・時間経過で自動的にぼかしに戻る)
- アプリがバックグラウンドに回った/タブが非アクティブになった/一定時間操作がなかった、のいずれかを検知したら、次に開いたときは必ずぼかし状態からスタートする(直前の状態を記憶・復元しない)
- **設定画面の基本給入力欄は常にマスク表示**(パスワード欄と同様)とし、目アイコンで表示/非表示を切り替えられるようにする
- 覗き見防止モード自体のオン/オフ切り替えは用意しない(常にオン)。設定項目を増やさず、常に安全側で固定する

---

## 12. データ保存設計(localStorage)

**個人データはサーバーには一切送信せず、その端末のブラウザ内(localStorage)だけに保存する。** 家族それぞれが自分のスマホ/PCのブラウザで開けば、それぞれ独立したデータになる(共有・同期はしない)。

| キー | 内容 |
|---|---|
| `wageSettings` | 基本給・所定労働時間・年間休日数・賃金締め日・法定休日の曜日・休憩時間帯(任意、未設定なら一律60分控除)・勤務間インターバルの目安時間・自動モードのON/OFF・標準勤務スケジュール(自動モード時) |
| `wageState` | 出退勤の状態・出勤時刻・前回退勤時刻・自動モードの残業開始フラグ |
| `wageCalendar` | 日付ごとのステータス(通常出勤の実績 / 有給休暇 / 会社休日 / 未入力)を持つマップ。今月・先月の日付分のみ保持 |
| `wageCurrentPeriodLog` | **今月(進行中の清算期間)** の日別実働時間・区分別内訳・金額の配列 |
| `wagePreviousPeriodLog` | **先月(直前に確定した清算期間)** の日別実働時間・区分別内訳・金額の配列。前月比較(7.2)に使用 |
| `wageOvertimeHistory` | 清算期間ごとの残業時間の合計だけを持つ軽量な配列(最大12ヶ月分)。36協定の複数月・年間チェック(7.4)に使用。日別内訳は含まない |

締め日を迎えるタイミングで、`wageCurrentPeriodLog` の内容を `wagePreviousPeriodLog` に上書きし、`wageCurrentPeriodLog` は空の状態で新しい清算期間を開始する。それまでの `wagePreviousPeriodLog`(前々月分の詳細ログ)は破棄されるが、その残業時間の合計だけは `wageOvertimeHistory` に追記されて残る。`wageCalendar` も同じタイミングで範囲外の日付を削除する。

### 12.1 何をするとデータがリセットされる(消える)か

以下のいずれかを行うと、保存されている設定・実績データはすべて消える。UI(設定画面など)にこの一覧をそのまま明記すること。

- ブラウザの「閲覧履歴データを削除」「サイトデータを削除」等の操作をした場合
- シークレットモード/プライベートブラウジングで使っていた場合(タブ・ウィンドウを閉じた時点で消える)
- ブラウザアプリ自体をアンインストールした場合
- 端末を機種変更・初期化した場合(自動的な引き継ぎはない)
- 普段と違うブラウザや別の端末で開いた場合(localStorageはブラウザ+端末ごとに独立)
- ブラウザのストレージ容量が逼迫し、自動的にサイトデータが削除された場合(頻度は低いが起こり得る)
- ホーム画面に追加したアイコンを削除して入れ直した場合(実装によっては再インストール扱いになることがある)

### 12.2 リセットされないこと

通常のページ再読み込み・ブラウザを閉じて再度開くだけの操作、スマホ本体の再起動、機内モード/オフラインでの使用、アプリ(ブラウザ)のアップデート — いずれもlocalStorageの中身には影響しない。

### 12.3 バックアップについて

現状の設計ではエクスポート/バックアップ機能を持たないため、12.1のいずれかが発生すると復元手段はない。将来拡張(13章)でCSVエクスポート等を検討する際、あわせてバックアップ/復元機能も候補に入れておくとよい。

---

## 13. 将来拡張(今回は実装しなくてよいが設計上考慮)

- 日別・月別の給与明細サマリー画面(CSVエクスポート)
- 複数プロファイル(家族の人数分の設定切り替え)
- 通勤手当・各種手当の加算
- (任意)未入力日をリマインドしてほしい人向けの、オンにできる通知設定(デフォルトはオフ=催促しない)
- 設定・実績データのバックアップ/復元機能(JSONエクスポート・インポート等)

---

## 14. Claude Codeへの実装指示メモ

**迷ったら1.1の三原則に立ち返る: 計算は極力正確に(裏側)、操作は単純に(出勤/退勤の2ボタンのみ、または自動モード)、表示はシンプルに(常時表示は3つの数字だけ)。仕事のわき役であり主役ではない。**

- 言語/技術: フレームワーク不要のHTML+JS単体ファイルでも良いし、規模が大きくなるならKotlin(Android Studio)や軽量フロントエンド構成に切り出しても良い。まずはWeb版(HTML+JS)を優先
- 3章の計算式をユニットテスト可能な純粋関数として切り出すこと(UIロジックと分離)
- 割増率の値は定数として1箇所にまとめ、将来就業規則に合わせて調整しやすくすること
- 「締め日から清算期間の開始日・終了日を算出する関数」「未入力日を洗い出して自動加算する関数」も、UIから独立した純粋関数として実装すること
- 休憩は打刻せず自動控除する(5.2)。実装は「その日の実働時間 = 出退勤の経過時間 − 控除分」というシンプルな関数1つで済み、`status`は`"off"|"working"`の2状態だけでよい
- 区分計算(所定内/法定内残業/深夜/休日の分解)が重くなっても、メインのアニメーションループ(数値の描画更新)には影響を与えないよう分離すること。区分計算は数百ms〜1秒間隔で再評価すれば十分
- 4.1のデフォルト値は初期設定画面のプレースホルダー/初期選択値として定数化し、いつでもユーザーが上書きできるようにすること
- 設定画面のどこかに「データはこの端末のブラウザ内にのみ保存されます」という一言と、12.1のリセット条件一覧を(折りたたみでよいので)必ず表示すること
- 11章の覗き見防止は「毎回デフォルトでぼかし状態」が肝。ページ読み込み時・visibilitychangeイベントのいずれでも、金額表示のstateは必ず「ぼかし」から始めること
- 1.2の「数秒で完結する」利用シーンに合わせ、外部フォント・ライブラリの読み込みは最小限にし、初回表示までの体感速度を最優先すること。残業時間帯に入った際の演出も、警告色や通知音ではなく色味の変化程度に留めること
- 締め日処理(清算期間のロールオーバー)の関数に、`wageOvertimeHistory`への追記処理(7.4)も必ず含めること。詳細ログを破棄する前に、残業時間合計を先に履歴配列へ追記してから破棄する順序を守ること
- 自動モード(5.3)は、手動モードの`calcEarnings`(5.4)をそのまま流用できるように設計するとよい。違いは「`clockInAt`をユーザーが押すか、スケジュールの出勤予定時刻から自動算出するか」だけに留め、金額計算のロジック自体は分岐させないこと。「残業開始」は内部的には「退勤予定時刻での自動確定を無効化するフラグを立てる」という状態遷移として扱うとシンプルになる。このフラグが立っていない状態で退勤予定時刻を迎えたら、そのまま自動確定する処理を必ず実行すること
